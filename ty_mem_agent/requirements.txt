# ===================================================================
# TY Memory Agent 依赖配置
# ===================================================================
#
# 推荐使用 Poetry 安装（环境隔离更好，版本锁定更可靠）：
#   poetry install --with ty-mem-agent --extras "mcp code_interpreter rag"
#
# 如果需要同步本文件（在 pyproject.toml 新增依赖后执行）：
#   poetry export -f requirements.txt \
#     --with ty-mem-agent \
#     --extras "mcp code_interpreter rag" \
#     --without-hashes \
#     -o ty_mem_agent/requirements.txt
#
# 传统 pip 安装（不推荐，无版本锁定）：
#   pip install -r requirements.txt
# ===================================================================

# ===================================================================
# 核心框架（qwen-agent 主包 + extras）
# ===================================================================
qwen-agent[gui,rag,code_interpreter,mcp]

# ===================================================================
# 主依赖组（pyproject.toml [tool.poetry.dependencies]）
# ===================================================================

# 文档解析
openpyxl

# JWT / 认证
PyJWT

# 时间解析
dateparser
jionlp
parsedatetime

# 飞书 SDK
lark-oapi

# ===================================================================
# TY Memory Agent 专用依赖（pyproject.toml [ty-mem-agent] 组）
# ===================================================================

# 日志系统
loguru

# HTTP 客户端和异步支持
httpx[http2]
aiohttp

# 数据库和缓存
redis

# 用户认证和安全
python-jose[cryptography]
passlib[bcrypt]

# 配置管理
python-dotenv
pyyaml
pydantic-settings

# 服务注册与发现（可选：如需通过 Nacos 发现 Dubbo 服务）
nacos-sdk-python

# 附件文本提取（文档解析 + 图片 OCR）
pdfminer-six
pdfplumber
python-docx
python-pptx

# ===================================================================
# 已包含在 qwen-agent extras 中的依赖（仅注释说明，无需重复安装）
# ===================================================================
# fastapi          (在 code_interpreter extra 中)
# uvicorn          (在 code_interpreter extra 中)
# pydantic         (在主依赖中)
# requests         (在主依赖中)
# mcp              (在 mcp extra 中)
# pandas           (在 rag / code_interpreter extra 中)
# pillow           (在 code_interpreter extra 中)
